<%= link_to "Back", exams_path %> ||
<% #= form_with(model: @user, url: user_session_path, remote: true) do |f| %>
  <% #= f.collection_select(:pyear, User.select(:pyear).distinct, :pyear, :pyear, :prompt => "Select Pass out Year") %>
<% # end %>
<%= render "form" %>
